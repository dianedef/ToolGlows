---
artifact: bug_record
metadata_schema_version: "1.0"
artifact_version: "0.1.0"
project: "ToolGlows"
created: "2026-09-02"
updated: "2026-09-03"
status: active
source_skill: "sg-fix"
scope: "auto-copy-selection-flow"
owner: "ToolGlows"
bug_id: "BUG-2026-09-02-001"
title: "Auto Copy can lose the selection, copy source metadata, and render unreadable feedback"
bug_status: "fixed-pending-verify"
severity: "high"
reported_by: "operator-requested audit"
first_observed: "2026-09-02"
last_observed: "2026-09-02"
environment: "Chrome and Firefox content script"
reproducibility: "static-path-confirmed"
redaction_status: "redacted"
related_bugs: []
related_artifacts:
  - "shipglows_data/technical/architecture.md"
confidence: high
risk_level: high
security_impact: yes
docs_impact: yes
evidence:
  - "Static flow inspection found raw selection logging, overlapping event triggers, success-styled error notifications, and settings-only activation."
  - "No Auto Copy regression test existed before this repair."
  - "Operator retest invalidated the initial closure: some real pages produced neither a clipboard write nor a notification."
  - "A second operator retest found transparent notification styling, lost selection feedback, and source URLs instead of the exact selected text."
  - "A follow-up experience audit found editable-field focus loss, silent iframe selections, misleading format controls, and an 808 ms Alt-mode delay on a 10,000-block page."
depends_on: []
supersedes: []
next_step: "Reload the unpacked extension and confirm Auto Copy on the operator's original page."
---

# Bug Record: BUG-2026-09-02-001

## Summary

- Title: Auto Copy can silently ignore host-page selections
- Current status: `fixed-pending-verify`
- Severity: `high`
- Owner: ToolGlows
- Next step: Reload the unpacked extension and confirm Auto Copy on the operator's original page.

## Reproduction

- Preconditions:
  - Auto Copy is active, or its settings panel is opened from an inactive toolbar button.
  - A non-empty page selection exists.
- Steps:
  1. Select text with the pointer, keyboard, shortcut, or Alt element-selection mode.
  2. Observe clipboard writes, notifications, and content-script console output.
  3. Exercise a clipboard failure and open settings while the registered tool remains inactive.

## Expected Behavior

- One completed user gesture produces at most one clipboard write and one matching notification.
- Selected or formatted content is never written to diagnostics.
- Error notifications use an error state, and settings-only access does not activate page copying.
- Formatting controls operate on the selected representation they describe.

## Observed Behavior

- Raw selected and formatted content is logged.
- `mouseup`, generic `keyup`, shortcut `keydown`, and Alt `click` paths can overlap.
- Both success and error messages are emitted with success severity.
- Mounting the settings component installs global copy listeners even when the registered tool is inactive.
- Formatting starts from `Selection.toString()`, so preserved HTML structure is unavailable.
- Generic host-page classes containing `p-` are treated as extension UI, so valid selections below common utility classes such as `p-4` return early with no copy and no notification.
- The automatic path applies format templates and the persisted `includeSource` setting, while its hidden-textarea fallback replaces the page selection; the teleported toast also falls outside the selectors that give ToolGlows UI an opaque layout.

## Evidence

- Redacted static inspection of the Auto Copy composable, store, settings component, and toolbar activation boundary.
- Baseline: typecheck passed; 160 existing tests passed; no dedicated Auto Copy test was present.
- Isolated MV3 load observed the service worker; content-script interaction remained untested.

## Diagnosis Notes

| Date UTC | Skill | Author | Hypothesis | Findings | Outcome |
|----------|-------|--------|------------|----------|---------|
| 2026-09-02 17:11:32 UTC | sg-fix | Codex | The feature lacks one canonical activation and gesture boundary. | Global listeners are mounted from the settings component; handlers do not consistently guard registered activation; overlapping events invoke the same copy path; notification severity is hard-coded; content values are logged. | supported |
| 2026-09-02 17:23:00 UTC | sg-verify | Codex | Automated and isolated-load evidence may verify the implementation, but not the native clipboard interaction in a real browser gesture. | Diff review, 7 focused regressions, 167 full tests, typecheck, both builds, manifest lint and isolated content-script loading pass. The isolated lab observes the scripts but does not select text or read the clipboard. | initially partial; superseded by the browser retest below |
| 2026-09-02 18:41:00 UTC | sg-fix | Codex | A host-page exclusion rule is swallowing ordinary selections before the clipboard and notification paths run. | The selector `[class*="p-"]` treats widespread utility classes as extension UI. The original browser test used a class-free page and preactivated the tool with an artificial clipboard grant, so it could not expose this branch. | supported; initial closure invalidated and repair narrowed to the ToolGlows root boundary |
| 2026-09-02 19:35:00 UTC | sg-fix | Codex | The remaining failures come from three independent integration mismatches hidden by the old assertions. | Automatic copying still applies source templates; the hidden fallback textarea steals the selection range; and the teleported PrimeVue toast is outside the scoped ToolGlows style boundary. Browser-computed styles confirmed a transparent background, `0.8` opacity and block layout before repair. | supported; repaired and awaiting operator confirmation |
| 2026-09-02 18:04:43 UTC | sg-verify | Codex | The repaired flow should copy exactly once through the native API, fall back cleanly when modern permission is absent, keep diagnostics redacted and show truthful feedback. | An isolated Chromium profile loaded the built MV3 extension. A real pointer selection copied the exact 14 selected characters with one success notification. With clipboard permissions cleared, the fallback copied successfully with one success notification. The total-failure error notification remains covered by the focused regression. | supported; verification passed, closure pending Git persistence |

## Fix Attempts

| Date UTC | Skill | Author | Changes | Validation | Result | Next Step |
|----------|-------|--------|---------|------------|--------|-----------|
| 2026-09-02 17:21:39 UTC | sg-fix | Codex | Guarded every trigger with registered activation and one in-flight copy; separated notification severities; removed content logging; derived preserved formatting from cloned ranges; cleaned fallback and Alt hover listeners; silenced disabled setting notifications; added focused regressions and architecture guidance. | `pnpm exec vitest run tests/auto-copy.test.ts`; `pnpm typecheck`; `pnpm exec vitest run`; `pnpm build`; `pnpm lint:manifest`; isolated MV3 target load. | passed | Verify the diff, proof coverage, documentation, and residual interactive-browser gap. |
| 2026-09-02 18:41:00 UTC | sg-fix | Codex | Replaced broad host-class exclusions with the ToolGlows DOM root as the ownership boundary; changed the browser regression to activate Auto Copy through the toolbar, avoid an artificial clipboard-write grant, and select below `p-4`. | 8 focused tests and rebuilt isolated Chromium interaction. | passed locally | Confirm on the operator's original page after extension reload. |
| 2026-09-02 19:35:00 UTC | sg-fix | Codex | Made automatic selection copy raw text only; preserved/restored the page range around the synchronous clipboard fallback; added active selection coloration; attached the teleported toast to the ToolGlows UI boundary and gave it an opaque grid layout. | 11 focused tests plus rebuilt Chromium clipboard, pseudo-selection and computed-toast assertions. | passed locally | Confirm exact clipboard text and rendered feedback on the operator's original page. |
| 2026-09-03 20:16:00 UTC | sg-fix | Codex | Replaced selection-stealing clipboard behavior with a copy-event-first adapter and complete focus/caret restoration; added pointer/touch and subframe coverage; excluded password fields; replaced the full-page Alt scan with one delegated overlay; clarified that formats apply to explicit shortcuts. | 16 focused checks, typecheck, rebuilt Chromium interaction, iframe clipboard/status proof, and 10,000-block Alt budget. | passed locally | Confirm the rebuilt extension on the operator's original page. |
| 2026-09-03 21:10:00 UTC | sg-fix | Codex | Added explicit idle, copying, confirmed and failed states; deduplicated unchanged pointer and keyboard selections; grouped notifications so feedback replaces instead of stacks; added a two-step selection fade followed by range cleanup at 1.5 seconds; aligned subframe cleanup and motion. | 19 focused checks, full suite, both builds, manifest lint, isolated extension load, and rebuilt Chromium state-transition proof. | passed locally | Confirm the rebuilt extension on the operator's original page. |

## Retest History

| Date UTC | Skill | Retest Command | Environment | Result | Notes |
|----------|-------|----------------|-------------|--------|-------|
| 2026-09-02 17:21:39 UTC | sg-test | Focused Auto Copy regression suite plus full repository baseline | jsdom and local build | passed | 7/7 focused scenarios; 167/167 total tests; typecheck and both browser builds passed. |
| 2026-09-02 17:21:39 UTC | sg-test | Isolated extension lab against `https://example.com` | temporary headless Chromium profile | partial | MV3 service worker and content scripts observed without content-script errors; the lab does not perform clipboard interaction. Popup still reports the pre-existing missing `vite.svg`. |
| 2026-09-02 18:04:43 UTC | sg-test | `node tests/auto-copy-browser.mjs` | temporary isolated Chromium profile | passed | Real pointer selection copied the exact 14-character selection once; one success notification appeared; diagnostics contained no selected content. Clearing modern clipboard permissions exercised the successful `execCommand` fallback with one notification. |
| 2026-09-02 18:41:00 UTC | sg-test | Toolbar activation plus pointer selection below `p-4`, without injected clipboard-write permission | rebuilt extension in a temporary isolated Chromium profile | passed | Exact clipboard content and one success toast observed; the test now exercises the previously silent exclusion branch. |
| 2026-09-02 19:35:00 UTC | sg-test | Exact-text selection with `includeSource: true`, synchronous fallback, selection pseudo-style and toast computed-style assertions | rebuilt extension in a temporary isolated Chromium profile | passed | Clipboard contains only the 14 selected characters; selection remains present with pink feedback; toast computes to white, opacity 1 and grid layout. |
| 2026-09-03 20:16:00 UTC | sg-test | Pointer selection, clipboard-denial fallback, same-origin subframe, tokenized selection feedback, and Alt activation on 10,000 blocks | rebuilt extension in a temporary isolated Chromium profile | passed | Exact top-frame and iframe text copied; opaque status appeared; diagnostics remained redacted; Alt event-loop delay measured 21 ms with zero inline outlines instead of the audited 808 ms and 10,002 mutations. |
| 2026-09-03 21:10:00 UTC | sg-test | Toast identity, unchanged-selection deduplication, progressive fade and range cleanup | rebuilt extension in a temporary isolated Chromium profile | passed | The original notification node remained after a redundant gesture; one notification was present; the fading state was observed; the browser selection and feedback state were gone after 1.5 seconds. |

## Related Bugs / Artifacts

- Related bugs: none known.
- Related artifacts: ToolGlows technical architecture.

## Lifecycle Notes

- The operator approved a bounded repair after the read-only audit.
- The operator's retest invalidated the initial closure. Automated and isolated-browser retests now pass against the identified real-page exclusion branch; operator confirmation on the original page remains pending.

## Redaction Status

- Current redaction status: `redacted`
- Redaction notes: No selected text, clipboard contents, cookies, credentials, or private page payloads are persisted.

## Closure Criteria

- Dedicated tests prove inactive/settings-only behavior, one copy per shortcut/Alt gesture, notification severity, formatting selection, and exceptional clipboard cleanup.
- Typecheck, complete tests, Chrome and Firefox builds, manifest lint, and isolated MV3 load pass.
- Verification confirms documentation alignment and no new permission; operator confirmation on the original page is still required before closure.

## Next Step

Reload the unpacked extension and confirm Auto Copy on the operator's original page.
