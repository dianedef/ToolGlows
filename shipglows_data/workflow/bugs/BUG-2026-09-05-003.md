---
artifact: bug_record
metadata_schema_version: "1.0"
artifact_version: "0.1.0"
project: ToolGlows
created: "2026-09-05"
updated: "2026-09-05"
status: active
source_skill: sg-bug
scope: rich-copy-dropdown-background
owner: ToolGlows
bug_id: BUG-2026-09-05-003
title: Rich Copy format menu has a transparent background
bug_status: fixed-pending-verify
severity: low
confidence: high
risk_level: low
security_impact: none
docs_impact: yes
evidence: ["Operator screenshot and isolated Chromium computed-style and selection proof."]
depends_on: []
supersedes: []
next_step: Operator can inspect the captured rendered menu and reload the rebuilt extension.
---

# Evidence-first repair

The operator identified the transparent menu background in the screenshot
`C:/Users/Diane/AppData/Local/Temp/codex-clipboard-2c4610a2-7e71-4124-843c-be0ddee11075.png`.
Rich Copy's dropdown teleports outside the dialog, but lacked the maintained
`toolglows-settings-select-panel` class. The theme scoper and semantic overlay
rules already recognize this class. Applying it restores the existing opaque
surface without new colors, permissions or copy behavior.

# Retest

`node tests/rich-copy-menu-browser.mjs` passed on the rebuilt Chrome extension
in a disposable Chromium profile. The menu background was `rgb(31, 41, 55)`,
opacity was `1`, and selecting HTML updated the displayed selection correctly.
Capture inspected by the agent:
`C:/Users/Diane/AppData/Local/Temp/toolglows-rich-copy-menu-fixed.png`.
Human rendered acceptance remains separate from this browser proof.
Chrome and Firefox builds completed successfully. The browser assertions and
screenshot completed before a slow browser shutdown required stopping the
dedicated disposable Chromium process; no personal profile was touched.

The initial automation attempt timed out before the toolbar appeared; it was
not a menu reproduction. The final runner uses a fresh test tab and waits for
the extension root. The scoped diff check passes. The global design drift scan
reports pre-existing findings across the dirty checkout; this one-property
change adds no visual literal and reuses the existing design-system boundary.
